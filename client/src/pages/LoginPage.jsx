import { motion } from "framer-motion"

export default function LoginPage() {
    return (
        <div>
            <motion.h1 layoutId="login-title" className="text-3xl font-bold text-gray-800 dark:text-slate-200">Login</motion.h1>
        </div>
    )
}